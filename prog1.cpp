#include <bits/stdc++.h>
using namespace std;

int32_t main() {
    vector<int> v;
    cout <<"Hello wrld! ";
    cout << "Dear Coder"<<endl;

    v.push_back(0);
    v.push_back(-5);
    v.push_back(10);
    for (auto it = v.begin(); it != v.end(); it++)
        cout << *it << " ";
}